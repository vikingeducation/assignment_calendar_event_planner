

<header class="page-header">
  <h1>Edit Calendar: {{ calendar.name }}</h1>
</header>


<form action="/calendar/{{ calendar.name }}" method="post">
  <input type="hidden" name="_method" value="PUT">

  <h2>Calendar Info</h2>
  <div class="form-group">
    <label for="calendar_name">Name</label>
    <input type="text" class="form-control" name="calendar[name]" id="calendar_name" value="{{ calendar.name }}">
  </div>
  <div class="form-group">
    <label for="calendar_userId">Last Name</label>
    <input type="text" class="form-control" name="calendar[userId]" id="calendar_userId" value="{{ calendar.userId }}">
</div>
  <div class="form-group">
    <input type="submit" class="btn btn-primary">
  </div>
</form>
