

<header class="page-header">
  <h1>Invitation Index</h1>
  <a href="/invitations/new" class="btn btn-primary">+ New Invitation</a>
</header>


{{#if invitations.length }}
  <table class="table">
    <thead>
      <tr>
        <th>Event Name</th>
        <th>Username</th>
        <th>Email</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each invitations as |invitation| }}
      <tr>
        <td>
          <a href="/events/{{ invitation.event_id }}">{{ invitation.Event.name }}</a>
        </td>
        <td>
          <a href="/users/{{ invitation.Event.Calendar.User.id }}">{{ invitation.Event.Calendar.User.username }}</a>
        </td>
        <td>
          {{ invitation.Event.Calendar.User.email }}
        </td>
        <td>
          <a href="/invitations/{{ invitation.id }}?_method=delete" class="text-danger">Delete</a>
        </td>
      </tr>
      {{/each }}
    </tbody>
  </table>
{{else }}
  <p class="text-danger">No Events</p>
{{/if }}
